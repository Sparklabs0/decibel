type Note @model @auth(rules: [{allow: owner}]) {
  id: ID!
  title: String
  text: String
  audio: [String!]!
  type: String! @index(name: "notesByDate", queryField: "notesByDate", sortKeyFields: ["createdAt"])
  createdAt: String!
  jsonData: AWSJSON 
  label: String 
}