type Note @model @auth(rules: [{allow: owner}]) {
  id: ID!
  title: String!
  audio: [String!]!
  createdAt: String!
  label: String
  transcription: String
  summary: AWSJSON
  favorited: Boolean
}
 